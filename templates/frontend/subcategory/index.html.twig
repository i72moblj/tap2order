{% extends 'layout.html.twig' %}

{% block body %}
    <h1>Sucategorías</h1>

    <h2>Lista de productos</h2>

    {% for product in products %}
        <p>
            {{ product.name }}
        </p>
    {% else %}
        <p>
            No products have been found.
        </p>
    {% endfor %}
{% endblock %}
{% extends 'layout.html.twig' %}

{% block body %}
    <h1>Products</h1>

    {% for product in products %}
        <p>
            {{ product.name }}
        </p>
    {% else %}
        <p>
            No products have been found.
        </p>
    {% endfor %}
{% endblock %}